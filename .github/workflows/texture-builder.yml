name: Generate Block Data

on:
  push:
    paths:
      - 'assets/**'          # Runs whenever files in assets/ change
      - 'texture-builder.py' # Runs if you update the script itself

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install Pillow

      - name: Run block generator script
        run: python texture-builder.py

      - name: Commit and push lowercase files and BlockData.json
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add assets/ BlockData.json
          git diff --staged --quiet || (git commit -m "Auto-lowercase assets and generate BlockData.json [skip ci]" && git push)
